source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in feedbacker.gemspec.
gemspec
gem 'puma', '~> 6.0'
gem 'redis'
gem 'redis-namespace'
gem 'simple_form'

gem 'diffy'
#gem 'kaminari'

gem "nio4r", ">= 2.7.0"
gem 'concurrent-ruby', '1.3.4'

group :development do
  gem 'sqlite3', '~> 2.0'
end
#group :production do
#  gem 'pg'
#end


# To use a debugger
# gem 'byebug', group: [:development, :test]
