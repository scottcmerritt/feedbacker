<div class="comment border my-1 fs-6 p-1">
      <%= tag.span comment.title, class: "fw-bold font-weight-bold fs-6" %>
      <div class="body">            
      <p class="d-flex align-items-top"><%= comment.body %><span class="ms-auto"><%= tag.span "#{time_ago_in_words(comment.created_at)} ago", class:"fs-7" %></span>
      </p>
      </div>
      <div class="text-end fs-6 text-secondary">
            Commented on <%= link_to comment.commentable.title, comment.commentable %>
      </div>
</div>
