<% translations = @translations if local_assigns[:translations].nil? %>
<%= paginate translations rescue "Error: PAGER not working" %>

<% translations.each do |row| %>
	<%= render partial: "#{engine_prefix}translates/parts/list_row", locals: {row: row} %>
<% end %>